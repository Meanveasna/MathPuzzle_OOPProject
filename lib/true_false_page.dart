import 'package:flutter/material.dart';

class TrueFalsePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('True/False')),
      body: Center(
        child: Text('True/False content here'),
      ),
    );
  }
}
